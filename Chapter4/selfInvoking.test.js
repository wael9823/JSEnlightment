const wish = require('./selfInvokingFunc');


test('Check if function wishes correctly',() => {
    expect(wish).toEqual('Happy Birthday Someone!');
});

test('Check if function wishes correctly',() => {
    expect(wish).toEqual('Happy Birthday Someone!');
});
